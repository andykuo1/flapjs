export function validateDFAGraph(graph)
{
  //Rules:
  //Each state must have an outgoing transition for each alphabet
  //Each state must not have more than 1 transition for each alphabet
  //Each edge must not be read EMPTY
}

export function validateNFAGraph(graph)
{
  //Rules:
  //None?
}
